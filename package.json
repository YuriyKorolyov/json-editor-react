{
  "name": "json-editor-root",
  "private": true,
  "scripts": {
    "build": "docker-compose build",
    "up": "docker-compose up",
    "seed": "docker-compose exec server npx prisma db seed"
  },
  "prisma": {
    "seed": "node server/prisma/seed.mjs"
  },
  "devDependencies": {
    "prisma": "^6.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2"
  }
}
